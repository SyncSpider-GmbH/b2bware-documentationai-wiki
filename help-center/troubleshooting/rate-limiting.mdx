---
title: Rate Limiting
description: DataHub's request rate limits, how to detect them, and how to back off gracefully.
---

## The Limit

DataHub applies **per-scope** rate limits rather than one global ceiling.
Traffic is classified into scopes and each scope has its own
request-per-minute budget **and** its own keying strategy, so one caller can
never exhaust another caller's budget:

| Scope | Default (req/min) | Keyed by |
|---|---|---|
| `admin` (authenticated admin API) | **300** | Authenticated user id (IP fallback) |
| `integration` (machine-to-machine `/apiv1/*` API) | **3000** | Authenticated user id (IP fallback) |
| `storefront` (server-rendered pages) | **120** | Shop customer → session → IP |
| `webhook` (payment/provider callbacks) | **120** | Installation key in the URL |
| `bulk` (export / import / AI ops) | **60** | Authenticated user id (IP fallback) |
| `auth_login` | **30** | IP |
| `auth_register` | **10** | IP |
| `password_reset` | **5** | IP |
| `public` (generic unauthenticated) | **60** | IP |

Ceilings are defined in `config/ratelimits.php` (the single source of truth)
and every value is env-overridable. See **[Rate Limiting](/core-concepts/rate-limiting)**
for the full table, the env variables, and the keying rationale.

The window is a sliding 60-second bucket. Crossing a scope's ceiling returns
`429 Too Many Requests` until that scope's bucket clears.

## Response Headers

Every API response carries the standard Laravel throttle headers:

| Header | Meaning |
|---|---|
| `X-RateLimit-Limit` | The ceiling for the **current scope** (e.g. `300` for admin). |
| `X-RateLimit-Remaining` | Requests left in this window. |
| `Retry-After` | Seconds until the bucket resets (only on `429`). |
| `X-RateLimit-Reset` | UNIX timestamp of the next reset (only on `429`). |

Watch `X-RateLimit-Remaining` proactively — when it drops below ~50, slow the integration down before you ever see a 429.

## Detecting a 429

```json
HTTP/1.1 429 Too Many Requests
Retry-After: 23
X-RateLimit-Limit: 300
X-RateLimit-Reset: 1731676820

{ "message": "Too Many Attempts." }
```

Respect `Retry-After`. Do **not** retry sooner — repeated 429s extend the lockout in some configurations.

## Backoff Strategy

A reasonable retry loop:

```js
async function requestWithBackoff(fn, maxAttempts = 5) {
  for (let attempt = 1; attempt <= maxAttempts; attempt++) {
    const res = await fn();
    if (res.status !== 429) return res;
    const wait = Number(res.headers.get('Retry-After') ?? Math.min(60, 2 ** attempt));
    await new Promise(r => setTimeout(r, wait * 1000));
  }
  throw new Error('Rate limit exhausted');
}
```

Rules of thumb:

1. **Always honor `Retry-After`.** Fall back to exponential (1s, 2s, 4s, 8s, …) only when missing.
2. **Cap at 60 seconds.** Anything longer suggests a misconfiguration on your side.
3. **Apply backoff per scope.** A 429 on one customer's token does not affect another's.
4. **Stop after 5 attempts.** Bubble the error up — keeping the queue alive against a wall is worse than failing fast.

## Reducing Request Volume

Most "rate limit" tickets are caused by a chatty integration, not by genuine load. Audit your patterns:

| Anti-pattern | Better |
|---|---|
| GET each product by id in a loop | One `GET /products?filter[id]=[{"condition":"IN","value":[...]}]` |
| Poll every second for order status | Subscribe to the `order_status_changed` event ([Webhooks](/core-concepts/webhooks)). |
| Fetch the full catalog every hour | Use `?sort=-_updated_at` + a stored cursor for delta sync. |
| POST/PUT one cart-item at a time | Upsert the cart with all items in one `PATCH /carts`. |
| Re-fetch categories/attributes per page render | Cache them client-side for the session (rarely change). |

## Bulk Endpoints

Use the bulk variants where they exist — each counts as **one** request regardless of payload size:

| Bulk endpoint | Replaces |
|---|---|
| `PUT /api/v1/apps/product-hub/products/bulk/update` | N × `PUT /products/{id}` |
| `DELETE /api/v1/apps/product-hub/products/bulk/delete` | N × `DELETE /products/{id}` |
| `POST /api/v1/apps/product-hub/stock/upsert/bulk` | N × stock writes |
| `POST /api/v1/apps/product-hub/prices/upsert/bulk` | N × price writes |
| `PUT /api/v1/apps/product-hub/translations/upsert/bulk` | N × translation writes |
| `PUT /api/v1/apps/product-hub/attributes/insert/bulk` | N × attribute creates |

## Concurrency

Most server-to-server traffic against the admin API uses the **`admin`** scope
— 300 req/min is **~5 req/s**. With 4 concurrent workers your safe throughput
is one request per worker every ~800 ms. Note that **`bulk`** operations
(export / import / AI) sit on a tighter 60 req/min budget, so batch them rather
than fanning out. Integration platforms syncing through the legacy `/apiv1/*`
surface use the far higher **`integration`** scope (3000 req/min, bulk paths
included) and are unaffected by the admin/bulk ceilings. If you need more
headroom, raise the relevant `RATELIMIT_*` variable in coordination
with the DataHub admin — the ceilings are server-configured in
`config/ratelimits.php`.

## Server-to-Server vs Storefront

The two use **different scopes**. A server-to-server integration hits the
`admin` scope (300 req/min, keyed by your **user id** — so authenticate rather
than relying on a shared IP). A storefront hits the `storefront` scope
(120 req/min, keyed by the logged-in customer, then browser session, then IP);
a single browser session is unlikely to reach it. Concentrate retries and bulk
operations behind one authenticated identity to keep the budget predictable.

<Callout kind="warning" title="High-volume webhook tenants">
Payment providers **retry webhooks on a 429**, which can delay order-status
updates. If a tenant receives a high volume of provider webhooks, raise
**`RATELIMIT_WEBHOOK`** for that environment so legitimate payment callbacks are
not throttled.
</Callout>

## Common Pitfalls

| Symptom | Cause | Fix |
|---|---|---|
| Random 429 in production after a deploy | Cold caches caused a burst of catalog re-fetches. | Warm caches with a single bulk request, not a fan-out. |
| One customer floods the API | Storefront polling `/me` every second. | Cache `/me` client-side; refresh on auth-relevant actions only. |
| Webhook handler 429s back to EventHub | Handler re-fetches data from DataHub for every event. | Cache the related data; or fetch lazily only when the payload is insufficient. |
| Bulk import stalls mid-way | One write per row instead of bulk. | Group up to ~500 rows per bulk call. |
| 429 right after token rotation | New + old tokens used in parallel doubled the rate. | Use one at a time. |

## Next Steps

<Columns cols={2}>
  <Card title="Idempotency" icon="repeat" href="/core-concepts/idempotency">
    Retry safely after a 429 without creating duplicates.
  </Card>
  <Card title="Bulk Operations" icon="layers" href="/advanced/bulk-operations">
    Full reference for the bulk endpoints.
  </Card>
  <Card title="Webhooks" icon="webhook" href="/core-concepts/webhooks">
    Stop polling; subscribe instead.
  </Card>
  <Card title="Error Handling" icon="triangle-alert" href="/core-concepts/error-handling">
    Distinguish 429 from other transient errors.
  </Card>
</Columns>
