---
title: NotificationHub
description: Multi-channel notification system with 120+ endpoints covering email, SMS, push, Slack, broadcast, and database channels.
---

## Overview

The NotificationHub is the most feature-rich hub with 120+ endpoints. It provides complete notification lifecycle management across multiple channels (email, SMS, push, Slack, broadcast, database), notification types with templates, user preferences, delivery attempt tracking, and a comprehensive analytics dashboard.

**Base URL:** `api/v1/apps/notification-hub/`

<Callout kind="info">
All endpoints require Bearer token authentication. See [Authentication](/authentication).
For filter syntax, pagination, and sorting, see [Common Patterns](/core-concepts/common-patterns).
</Callout>

## Supported Channels

| Channel | Description |
|---------|-------------|
| `email` | Email delivery |
| `database` | In-app database storage |
| `broadcast` | Real-time broadcasting (WebSocket) |
| `sms` | SMS text message |
| `push` | Push notification |
| `slack` | Slack message |

## Endpoints

<Tabs>
  <Tab title="Notifications" icon="bell">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/notifications` | List notifications (paginated, filterable) |
| POST | `/notifications` | Create a notification |
| GET | `/notifications/{id}` | Get a single notification |
| PUT | `/notifications/{id}` | Update a notification |
| DELETE | `/notifications/{id}` | Delete a notification |
| POST | `/notifications/{id}/send` | Send/dispatch a notification |
| POST | `/notifications/{id}/resend` | Resend a failed notification |
| POST | `/notifications/{id}/mark-read` | Mark notification as read |
| GET | `/notifications/stats` | Notification statistics |
| GET | `/notifications/unread` | Get unread notifications |
| POST | `/notifications/create-for-user` | Create for a specific user |
| POST | `/notifications/create-bulk` | Bulk create notifications (max 10,000) |
| POST | `/notifications/create-bulk-for-roles` | Bulk create for role members |
| POST | `/notifications/mark-all-read` | Mark all as read |

  </Tab>
  <Tab title="Types" icon="tag">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/types` | List notification types |
| POST | `/types` | Create a notification type |
| GET | `/types/{id}` | Get a single type |
| PUT | `/types/{id}` | Update a type |
| DELETE | `/types/{id}` | Delete a type |
| GET | `/types/active` | List active types |
| GET | `/types/user-configurable` | List user-configurable types |
| GET | `/types/channel/{channel}` | List types for a channel |
| GET | `/types/stats` | Type statistics |
| GET | `/types/variables` | List all template variables |
| GET | `/types/variables/{slug}` | Get variables for a type |
| POST | `/types/variables/validate` | Validate template variables |
| POST | `/types/create-predefined` | Create a predefined type |
| POST | `/types/search` | Search notification types |
| POST | `/types/validate-template` | Validate a template |
| POST | `/types/bulk-activate` | Bulk activate types |
| POST | `/types/bulk-deactivate` | Bulk deactivate types |
| POST | `/types/roles` | Get types by role codes |
| GET | `/types/role/{roleCode}` | Get types for a role |
| POST | `/types/{id}/activate` | Activate a type |
| POST | `/types/{id}/deactivate` | Deactivate a type |
| POST | `/types/{id}/add-channel` | Add a channel |
| POST | `/types/{id}/remove-channel` | Remove a channel |
| POST | `/types/{id}/update-template` | Update the template |
| POST | `/types/{id}/add-role` | Add a role |
| POST | `/types/{id}/remove-role` | Remove a role |
| POST | `/types/{id}/set-roles` | Set roles (replaces all) |
| POST | `/types/{id}/make-global` | Make globally scoped |
| POST | `/types/{id}/make-specific` | Make specifically scoped |

  </Tab>
  <Tab title="Preferences" icon="settings">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/preferences` | List all preferences |
| POST | `/preferences` | Create a preference |
| GET | `/preferences/{id}` | Get a single preference |
| PUT | `/preferences/{id}` | Update a preference |
| DELETE | `/preferences/{id}` | Delete a preference |
| GET | `/preferences/stats` | Preference statistics |
| GET | `/preferences/channel-stats` | Channel preference stats |
| GET | `/preferences/for-user` | Get preferences for a user |
| GET | `/preferences/enabled-for-user` | Get enabled preferences |
| GET | `/preferences/users-for-digest` | Get digest subscribers |
| GET | `/preferences/users-in-quiet-hours` | Get users in quiet hours |
| POST | `/preferences/enable-for-user` | Enable a preference |
| POST | `/preferences/disable-for-user` | Disable a preference |
| POST | `/preferences/add-channel-for-user` | Add a channel |
| POST | `/preferences/remove-channel-for-user` | Remove a channel |
| POST | `/preferences/set-frequency-for-user` | Set frequency |
| POST | `/preferences/set-quiet-hours-for-user` | Set quiet hours |
| POST | `/preferences/bulk-update-for-user` | Bulk update preferences |
| POST | `/preferences/reset-to-defaults` | Reset to defaults |
| POST | `/preferences/create-defaults-for-user` | Create default prefs |
| POST | `/preferences/export-user-preferences` | Export preferences |
| POST | `/preferences/import-user-preferences` | Import preferences |
| POST | `/preferences/migrate-user-preferences` | Migrate between versions |

  </Tab>
  <Tab title="Attempts" icon="refresh-cw">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/attempts` | List all delivery attempts |
| POST | `/attempts` | Create an attempt record |
| GET | `/attempts/{id}` | Get a single attempt |
| PUT | `/attempts/{id}` | Update an attempt |
| DELETE | `/attempts/{id}` | Delete an attempt |
| GET | `/attempts/stats` | Attempt statistics |
| GET | `/attempts/pending` | List pending attempts |
| GET | `/attempts/ready-for-retry` | List ready for retry |
| GET | `/attempts/failures` | List all failures |
| GET | `/attempts/recent-failures` | List recent failures |
| GET | `/attempts/successful` | List successful attempts |
| GET | `/attempts/channel/{channel}` | List by channel |
| GET | `/attempts/channel-stats` | Stats per channel |
| GET | `/attempts/success-rates` | Success rates |
| GET | `/attempts/error-codes` | List error codes |
| GET | `/attempts/average-retry-time` | Average retry time |
| GET | `/attempts/notification/{notificationId}` | List for a notification |
| POST | `/attempts/{id}/mark-processing` | Mark as processing |
| POST | `/attempts/{id}/mark-success` | Mark as successful |
| POST | `/attempts/{id}/mark-failed` | Mark as failed |
| POST | `/attempts/{id}/mark-retry` | Mark for retry |
| POST | `/attempts/retry-failed` | Retry all failed |
| POST | `/attempts/clean-old` | Clean old records |

  </Tab>
  <Tab title="Dashboard & Settings" icon="bar-chart">

**Dashboard**

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/dashboard/overview` | Overall statistics |
| GET | `/dashboard/channel-performance` | Performance by channel (query: `days` 1-365) |
| GET | `/dashboard/recent-activity` | Recent activity (query: `hours` 1-168) |
| GET | `/dashboard/error-analysis` | Failure analysis |
| GET | `/dashboard/user-engagement` | User engagement metrics |
| GET | `/dashboard/system-health` | System health indicators |
| GET | `/dashboard/trends` | Notification trends |
| GET | `/dashboard/alerts` | Active system alerts |

**Settings**

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/settings/groups/list` | List settings groups |
| GET | `/settings` | Get all settings |
| POST | `/settings` | Update settings |
| POST | `/settings/reset` | Reset all to defaults |
| GET | `/settings/{group}` | Get settings for a group |
| POST | `/settings/{group}` | Update group settings |
| POST | `/settings/{group}/test` | Test a channel connection |
| POST | `/settings/{group}/reset` | Reset a group to defaults |

  </Tab>
</Tabs>

## Filters

For filter syntax and operators, see [Filtering](/core-concepts/filtering).

<Expandable title="Notification Filters" default-open="true">

| Filter Field | Type | Notes |
|--------------|------|-------|
| `id` | number | |
| `notification_type_id` | number | |
| `status` | text | `pending`, `sent`, `failed`, `partial`, `read` |
| `priority` | number | 1 (lowest) to 10 (highest) |
| `notifiable_type` | text | Polymorphic type (e.g., `Customers`, `Users`) |
| `notifiable_id` | number | |
| `creator_type` | text | Polymorphic type |
| `creator_id` | number | |
| `channels` | text | email, database, broadcast, sms, push, slack |

</Expandable>

<Expandable title="Notification Type Filters" default-open="false">

| Filter Field | Type | Notes |
|--------------|------|-------|
| `id` | number | |
| `name` | text | |
| `slug` | text | |
| `is_active` | bool | |
| `is_user_configurable` | bool | |
| `supported_channels` | json_array | Array of channel strings |
| `scope_type` | text | `global`, `specific` |

</Expandable>

<Expandable title="Preference Filters" default-open="false">

| Filter Field | Type | Notes |
|--------------|------|-------|
| `id` | number | |
| `user_type` | text | Polymorphic type |
| `user_id` | number | |
| `notification_type_id` | number | |
| `is_enabled` | bool | |
| `frequency` | text | `immediate`, `daily_digest`, `weekly_digest`, `disabled` |
| `channels` | text | |

</Expandable>

<Expandable title="Attempt Filters" default-open="false">

| Filter Field | Type | Notes |
|--------------|------|-------|
| `id` | number | |
| `notification_id` | number | |
| `channel` | text | email, sms, push, slack, broadcast, database |
| `status` | text | `pending`, `processing`, `success`, `failed`, `retry` |
| `attempt_number` | number | |
| `error_code` | text | |
| `attempted_at` | date | |
| `notification.notification_type_id` | number | Relation filter (notification) |

</Expandable>

## Includes

For include syntax, see [Includes & Relations](/core-concepts/includes).

| Resource | Available Includes |
|----------|--------------------|
| **Notifications** | `notificationType`, `notifiable`, `creator`, `attempts` |
| **Notification Types** | `notifications` |
| **Preferences** | `notificationType`, `user` |
| **Attempts** | `notification`, `notification.notificationType` |

## Validation Rules

<Expandable title="Create Notification" default-open="true">

| Field | Rules |
|-------|-------|
| `notification_type_id` | required, integer |
| `notifiable_type` | required, string |
| `notifiable_id` | required, integer |
| `title` | required, string, max:255 |
| `message` | required, string |
| `channels` | sometimes, array, each in: email, database, broadcast, sms, push, slack |
| `priority` | sometimes, integer, min:1, max:10 |
| `data` | sometimes, array |

</Expandable>

<Expandable title="Create Notification Type" default-open="false">

| Field | Rules |
|-------|-------|
| `name` | required, string, max:255 |
| `slug` | nullable, string, unique |
| `supported_channels` | sometimes, array, each in: email, database, broadcast, sms, push, slack |
| `default_channels` | sometimes, array |
| `is_active` | sometimes, boolean |
| `is_user_configurable` | sometimes, boolean |
| `scope_type` | sometimes, in: global, specific |
| `role_codes` | nullable, array |

</Expandable>

<Expandable title="Create Preference" default-open="false">

| Field | Rules |
|-------|-------|
| `user_type` | required, string |
| `user_id` | required, integer |
| `notification_type_id` | required, integer |
| `channels` | sometimes, array, each in: email, database, broadcast, sms, push, slack |
| `is_enabled` | sometimes, boolean |
| `frequency` | sometimes, in: immediate, daily_digest, weekly_digest, disabled |
| `quiet_hours_start` | nullable, string, regex: HH:MM |
| `quiet_hours_end` | nullable, string, regex: HH:MM |
| `timezone` | nullable, string (valid timezone) |
| `settings` | sometimes, array |

</Expandable>

<Expandable title="Create Attempt" default-open="false">

| Field | Rules |
|-------|-------|
| `notification_id` | required, integer |
| `channel` | required, in: email, database, broadcast, sms, push, slack |
| `status` | sometimes, in: pending, processing, success, failed, retry |
| `attempt_number` | sometimes, integer, min:1 |
| `error_code` | nullable, string |
| `error_message` | nullable, string |
| `attempted_at` | nullable, date |

</Expandable>

## Examples

<Steps>
  <Step title="List failed notifications with includes">
    <Request tabs="cURL">
```bash
curl -s -X GET \
  'https://api.datahub.syncspider.com/api/v1/apps/notification-hub/notifications?filter[status]=[{"condition":"=","value":"failed"}]&include=notificationType,attempts,creator&per_page=20&sort=-_created_at' \
  -H 'Authorization: Bearer {token}' \
  -H 'Accept: application/json'
```
    </Request>
  </Step>
  <Step title="Create a notification for a specific user">
    <Request tabs="cURL">
```bash
curl -s -X POST \
  'https://api.datahub.syncspider.com/api/v1/apps/notification-hub/notifications/create-for-user' \
  -H 'Authorization: Bearer {token}' \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json' \
  -d '{
    "notification_type_id": 5,
    "notifiable_type": "Customers",
    "notifiable_id": 1042,
    "title": "Order Shipped",
    "message": "Your order #ORD-2024-8847 has been shipped.",
    "channels": ["email", "database", "push"],
    "priority": 7,
    "data": {
      "order_id": 8847,
      "tracking_number": "1Z999AA10123456784",
      "carrier": "UPS"
    }
  }'
```
    </Request>
  </Step>
  <Step title="Get dashboard overview">
    <Request tabs="cURL">
```bash
curl -s -X GET \
  'https://api.datahub.syncspider.com/api/v1/apps/notification-hub/dashboard/overview' \
  -H 'Authorization: Bearer {token}' \
  -H 'Accept: application/json'
```
    </Request>
  </Step>
  <Step title="Filter delivery attempts by channel">
    <Request tabs="cURL">
```bash
curl -s -X GET \
  'https://api.datahub.syncspider.com/api/v1/apps/notification-hub/attempts?filter[channel]=[{"condition":"=","value":"email"}]&filter[status]=[{"condition":"IN","value":["failed","retry"]}]&include=notification,notification.notificationType&sort=-attempted_at&per_page=50' \
  -H 'Authorization: Bearer {token}' \
  -H 'Accept: application/json'
```
    </Request>
  </Step>
  <Step title="Test a notification channel connection">
    <Request tabs="cURL">
```bash
curl -s -X POST \
  'https://api.datahub.syncspider.com/api/v1/apps/notification-hub/settings/email/test' \
  -H 'Authorization: Bearer {token}' \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json' \
  -d '{
    "recipient": "admin@example.com",
    "subject": "Test Notification",
    "message": "This is a test to verify the email channel configuration."
  }'
```
    </Request>
  </Step>
</Steps>

## Settings Groups

NotificationHub channel settings are edited in the admin under **NotificationHub → Settings** (also available via the `/settings` endpoints in the Dashboard & Settings tab). Groups: `global`, `email_branding`, `email`, `sms`, `push`, `slack`, `broadcast`, `database`. See also [Hub Settings](/core-concepts/hub-settings).

<Expandable title="global — Settings Reference" default-open="true">

**global** — Global Settings

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `default_channels` | select (multi) | — | Default delivery channels: `email`, `database`, `sms`, `push`, `slack`, `broadcast`. |
| `fallback_channel` | select (`database` / `email`) | — | Channel used when primary delivery fails. |
| `quiet_hours_enabled` | boolean | `false` | Suppress non-urgent notifications during quiet hours. |
| `default_quiet_hours_start` | text | — | Quiet-hours start time (`HH:MM`, e.g. `22:00`). |
| `default_quiet_hours_end` | text | — | Quiet-hours end time (`HH:MM`, e.g. `08:00`). |
| `default_timezone` | text | — | Timezone for quiet hours (e.g. `UTC`). |

</Expandable>

<Expandable title="email_branding — Settings Reference" default-open="false">

**email_branding** — Email Branding

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `logo_url` | media-library | — | Logo image from Media Library (`public_url`). Max 2 MB; up to 400×200. |
| `show_header_icon` | boolean | `false` | Show an emoji icon in the header when no logo is set. |
| `header_icon` | text | `📧` | Emoji displayed in the email header. |
| `show_header_title` | boolean | `true` | Show the header title. |
| `default_header_title` | text | — | Fallback title when the notification has none. Supports `@t('...')` translation. |
| `show_header_subtitle` | boolean | `false` | Show the header subtitle. |
| `default_header_subtitle` | text | — | Default subtitle. Supports `@t('...')` translation. |
| `header_background_color` | text | `#667eea` | Header background / gradient start color. |
| `header_background_color_secondary` | text | `#764ba2` | Header gradient end color. |
| `header_text_color` | text | `#ffffff` | Header text color. |
| `hide_notification_type` | boolean | `false` | Hide the notification-type badge at the top of the email body. |
| `button_color` | text | `#667eea` | CTA button background color. |
| `button_text_color` | text | `#ffffff` | CTA button text color. |
| `show_thank_you_message` | boolean | `true` | Show a “Thank you!” line at the end of the email body. |
| `company_name` | text | — | Company name shown in the footer. |
| `footer_text` | rich-text | `This is an automated notification. Please do not reply to this email.` | Footer body. Supports `@t('...')` translation. |
| `footer_links` | array of `{ label, url }` | — | Footer links. `label` supports `@t('...')`. |
| `social_links` | object | — | Social profile URLs: `facebook`, `twitter`, `instagram`, `linkedin`. |

</Expandable>

<Expandable title="email — Settings Reference" default-open="false">

**email** — Email Channel

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable the email channel. |
| `driver` | select (`smtp` / `sendmail` / `mailgun` / `ses` / `postmark` / `log`) | — | Mail transport driver. |
| `host` | text | — | SMTP host (SMTP driver; root-level for backward compatibility). |
| `port` | number | — | SMTP port (e.g. `587`). |
| `encryption` | select (`none` / `tls` / `ssl`) | — | SMTP encryption. |
| `username` | text | — | SMTP username / account identifier. |
| `password` | password | — | SMTP password. Store the tenant secret; do not commit real values. |
| `mailgun_settings` | object | — | Mailgun credentials when `driver=mailgun`: <ul><li>`domain` — Mailgun sending domain</li><li>`secret` — API key (password)</li><li>`endpoint` — `api.mailgun.net` (US) or `api.eu.mailgun.net` (EU)</li></ul> |
| `ses_settings` | object | — | Amazon SES credentials when `driver=ses`: <ul><li>`key` — AWS Access Key ID</li><li>`secret` — AWS Secret Access Key (password)</li><li>`region` — AWS region (e.g. `us-east-1`, `eu-central-1`)</li></ul> |
| `postmark_settings` | object | — | Postmark credentials when `driver=postmark`: <ul><li>`token` — Server API token (password)</li><li>`message_stream` — Message stream (default `outbound`)</li></ul> |
| `sendmail_settings` | object | — | Sendmail path when `driver=sendmail`: <ul><li>`path` — Binary path (e.g. `/usr/sbin/sendmail -bs -i`)</li></ul> |
| `from_settings` | object | — | From / Reply-To (all drivers): <ul><li>`from_address` — Sender address (required when enabled)</li><li>`from_name` — Sender display name</li><li>`reply_to_address` — Reply-To address</li><li>`reply_to_name` — Reply-To display name</li></ul> |
| `test_mode` | boolean | `false` | Route sends to the test address instead of real recipients. |
| `test_email` | text | — | Destination when test mode is on. |

</Expandable>

<Expandable title="sms — Settings Reference" default-open="false">

**sms** — SMS Channel

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable the SMS channel. |
| `provider` | select (`twilio` / `nexmo` / `messagebird` / `plivo` / `infobip`) | — | SMS provider. |
| `from_number` | text | — | Sender phone number / alphanumeric ID. |
| `twilio_settings` | object | — | Twilio credentials: <ul><li>`account_sid` — Account SID</li><li>`auth_token` — Auth token (password)</li><li>`messaging_service_sid` — Optional Messaging Service SID</li></ul> |
| `nexmo_settings` | object | — | Nexmo/Vonage credentials: <ul><li>`api_key` — API key</li><li>`api_secret` — API secret (password)</li></ul> |
| `messagebird_settings` | object | — | MessageBird credentials: <ul><li>`access_key` — Access key (password)</li></ul> |
| `plivo_settings` | object | — | Plivo credentials: <ul><li>`auth_id` — Auth ID</li><li>`auth_token` — Auth token (password)</li></ul> |
| `infobip_settings` | object | — | Infobip credentials: <ul><li>`base_url` — API base URL</li><li>`api_key` — API key (password)</li></ul> |
| `general_settings` | object | — | Delivery constraints: <ul><li>`webhook_url` — Delivery-status webhook</li><li>`country_codes_allowed` — Allowed codes (empty = all)</li><li>`country_codes_blocked` — Blocked codes</li></ul> |
| `test_mode` | boolean | `false` | Route SMS to the test number. |
| `test_number` | text | — | Destination phone when test mode is on. |

</Expandable>

<Expandable title="push — Settings Reference" default-open="false">

**push** — Push Notifications

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable the push channel. |
| `provider` | select (`firebase` / `onesignal` / `pusher_beams`) | — | Push provider. |
| `firebase_settings` | object | — | Firebase (FCM) when `provider=firebase`: <ul><li>`project_id` — Firebase project ID</li><li>`credentials_json` — Service account JSON (secret)</li></ul> |
| `onesignal_settings` | object | — | OneSignal when `provider=onesignal`: <ul><li>`app_id` — App ID</li><li>`api_key` — REST API key (password)</li></ul> |
| `pusher_beams_settings` | object | — | Pusher Beams when `provider=pusher_beams`: <ul><li>`instance_id` — Instance ID</li><li>`secret_key` — Secret key (password)</li></ul> |
| `apns_settings` | object | — | Optional direct Apple APNs (iOS): <ul><li>`key_id` — APNs key ID</li><li>`team_id` — Apple Developer Team ID</li><li>`key_file` — Path to `.p8` key file</li><li>`production` — Use production APNs (`false` by default)</li></ul> |
| `general_settings` | object | — | Shared push options: <ul><li>`default_ttl_seconds` — Default TTL (e.g. `86400`)</li></ul> |
| `test_mode` | boolean | `false` | Route pushes to the test device token. |
| `test_device_token` | text | — | Device token used in test mode. |

</Expandable>

<Expandable title="slack — Settings Reference" default-open="false">

**slack** — Slack Channel

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable the Slack channel. |
| `webhook_url` | text | — | Incoming webhook URL. |
| `bot_token` | password | — | Bot OAuth token (`xoxb-…`). Store the tenant secret; do not commit real values. |
| `default_channel` | text | — | Default Slack channel (e.g. `#notifications`). |
| `username` | text | — | Bot display name. |
| `icon_emoji` | text | — | Bot icon emoji (e.g. `:bell:`). |
| `icon_url` | text | — | Bot icon image URL. |
| `unfurl_links` | boolean | `false` | Unfurl URLs in messages. |
| `unfurl_media` | boolean | `false` | Unfurl media in messages. |
| `test_mode` | boolean | `false` | Route messages to the test channel. |
| `test_channel` | text | — | Channel used in test mode. |

</Expandable>

<Expandable title="broadcast — Settings Reference" default-open="false">

**broadcast** — Broadcast Channel (WebSocket)

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `false` | Enable the broadcast channel. |
| `driver` | select (`pusher` / `redis` / `ably` / `log`) | — | Broadcast driver. |
| `pusher_settings` | object | — | Pusher when `driver=pusher`: <ul><li>`app_id` — App ID</li><li>`app_key` — App key</li><li>`app_secret` — App secret (password)</li><li>`cluster` — Cluster (e.g. `mt1`, `eu`, `ap1`)</li><li>`encrypted` — TLS (`true` by default)</li></ul> |
| `redis_settings` | object | — | Redis when `driver=redis`: <ul><li>`connection` — Laravel Redis connection name</li></ul> |
| `ably_settings` | object | — | Ably when `driver=ably`: <ul><li>`key` — API key (password)</li></ul> |
| `general_settings` | object | — | Shared options: <ul><li>`channel_prefix` — Prefix for broadcast channel names</li></ul> |
| `test_mode` | boolean | `false` | Enable test-mode broadcasting. |

</Expandable>

<Expandable title="database — Settings Reference" default-open="false">

**database** — Database Channel (Always Active)

Database storage is always on; these keys control retention and in-app behaviour. Maintenance runs daily via `notificationhub:maintenance`.

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `auto_cleanup` | boolean | `false` | Delete old notifications based on retention. |
| `retention_days` | number | `90` | Age in days after which notifications are deleted (1–365). |
| `cleanup_batch_size` | number | `1000` | Notifications deleted per maintenance batch (100–10000). |
| `max_per_user` | number | `0` | Max notifications kept per user (`0` = unlimited); oldest removed first. |
| `mark_read_on_view` | boolean | `false` | Mark as read on `GET /notifications/{id}`. |
| `group_similar` | boolean | `false` | Group same-type notifications to the same user within a time window. |
| `group_time_window_minutes` | number | `60` | Grouping window in minutes (1–1440). |

</Expandable>

## Related

<Columns cols={2}>
  <Card title="CustomerHub" icon="users" href="/hubs/customer-hub">
    Customers are the primary notification recipients.
  </Card>
  <Card title="OrderHub" icon="shopping-cart" href="/hubs/order-hub">
    Order events trigger notifications (shipped, completed, etc.).
  </Card>
</Columns>
