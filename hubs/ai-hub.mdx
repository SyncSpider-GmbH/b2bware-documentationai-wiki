---
title: AiHub
description: Shared AI wrapper — knowledge base, Platform Assist, models, usage logs, and App Settings for provider and assistants.
---

## Overview

AiHub centralizes AI credentials and defaults for the installation. Other hubs call the shared AI wrapper. This page covers knowledge CRUD, Platform Assist sessions, observability endpoints, and the **`general`** App Settings group.

**Base URL:** `api/v1/apps/ai-hub/`

<Callout kind="info">
All endpoints require Bearer token authentication (admin access). See [Authentication](/authentication).
For filter syntax, pagination, and sorting, see [Common Patterns](/core-concepts/common-patterns).
</Callout>

<Callout kind="info" title="Full API reference">
For request/response schemas, see the [AiHub API Reference](/api-reference/ai-hub).
</Callout>

## Endpoints

<Tabs>
  <Tab title="Knowledge" icon="book">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/knowledge` | List knowledge entries |
| POST | `/knowledge` | Create a knowledge entry |
| GET | `/knowledge/{id}` | Get knowledge entry details |
| PUT | `/knowledge/{id}` | Update a knowledge entry |
| DELETE | `/knowledge/{id}` | Delete a knowledge entry |

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/context` | Resolved AI context (schema + operations + knowledge) |
| GET | `/models` | Provider model catalog for dropdowns |

  </Tab>
  <Tab title="Platform Assist" icon="message-circle">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/assist/status` | Assist availability / status |
| POST | `/assist/context-preview` | Preview injected context |
| GET | `/assist/sessions` | List chat sessions |
| POST | `/assist/sessions` | Create a chat session |
| DELETE | `/assist/sessions` | Delete all sessions |
| GET | `/assist/sessions/{id}` | Get a session |
| DELETE | `/assist/sessions/{id}` | Delete a session |
| POST | `/assist/sessions/{id}/messages` | Post a message |
| POST | `/assist/sessions/{id}/tools/confirm` | Confirm a pending tool call |
| POST | `/assist/intake/uploads` | Upload intake file |
| POST | `/assist/intake/uploads/{uploadId}/process` | Process an intake upload |
| POST | `/assist/intake/uploads/{uploadId}/resolve` | Resolve an intake gap |

  </Tab>
  <Tab title="Observability" icon="activity">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/usage-logs` | List AI usage logs |
| GET | `/debug/requests` | Debug recent AI requests (superadmin-gated) |

  </Tab>
  <Tab title="Settings" icon="settings">

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/settings/groups/list` | List settings groups |
| GET | `/settings` | Get all settings |
| POST | `/settings` | Update settings |
| POST | `/settings/reset` | Reset all groups to defaults |
| GET | `/settings/{group}` | Get one group (`general`) |
| POST | `/settings/{group}` | Update one group |
| POST | `/settings/{group}/reset` | Reset one group |

See also [Hub Settings](/core-concepts/hub-settings).

  </Tab>
</Tabs>

## Settings

Group: `general`. Secrets such as `api_key` are masked on read.

<Expandable title="general — Settings Reference" default-open="true">

**general** — AI Configuration

| Setting | Type | Default | Description |
|---------|------|---------|-------------|
| `enabled` | boolean | `true` | Master switch. When off, the AI wrapper refuses all calls. |
| `b2bware_ai_enabled` | boolean | `true` | Use the managed b2bWare AI account (overrides provider selection; usage-billed). |
| `provider` | select (`openai` / `anthropic` / `google` / `ollama` / `custom_url`) | `openai` | AI service when b2bWare AI is off. |
| `custom_endpoint` | text | `""` | Base URL for OpenAI-compatible APIs when provider is `custom_url`. |
| `api_key` | secret (masked) | `""` | Optional BYOK provider API key for OpenAI / Anthropic / Google. Stored securely; never returned in plain text. |
| `model` | text | `gpt-4o-mini` | Default model name; callers may override per request. |
| `global_instruction` | textarea | helpful B2Bware admin assistant prompt | System instruction prepended to every AI request. |
| `context_prompt` | textarea | page-context guidance | Extra text appended to the injected client context block. |
| `admin_assistant_enabled` | boolean | `true` | Platform Assist for admin users (may be overridden by `PLATFORM_ASSIST_ENABLED`). |
| `storefront_assistant_enabled` | boolean | `false` | Customer-facing storefront assistant. |
| `chat_history_retention_days` | select (`7` / `30` / `90` / `365` / `unlimited`) | `30` | How long Platform Assist chat sessions are kept. |

</Expandable>
