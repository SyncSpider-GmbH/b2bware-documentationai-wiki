---
title: AuthHub
description: Authentication, registration, password resets, email verification, and sales-agent impersonation for storefronts and admin clients.
---

## Overview

AuthHub issues and manages user sessions. It is the entry point for storefront customers (login, register, forgot password, verify email) and for sales agents who impersonate a customer to place orders on their behalf.

**Base URL:** `api/v1/apps/auth-hub/`

<Callout kind="info">
Most endpoints sit behind `auth:api`. The two public flows — `reset-password` and `verify-email` — use the installation key in the URL instead of a bearer token, because the user is not logged in yet.
</Callout>

## Endpoints

<Tabs>
  <Tab title="Session" icon="key">

| Method | Endpoint | Auth | Description |
|---|---|---|---|
| POST | `/login` | `auth:api` | Exchange credentials for a personal access token. |
| POST | `/register` | `auth:api` | Create a new customer account. |
| POST | `/logout` | `auth:api` | Revoke the current token. |
| GET  | `/me` | `auth:api` | Return the authenticated user with roles and customer profile. |

  </Tab>
  <Tab title="Password & Email" icon="envelope">

| Method | Endpoint | Auth | Description |
|---|---|---|---|
| POST | `/forgot-password` | `auth:api` | Send a password-reset email. |
| POST | `/{installationKey}/reset-password` | installation key | Complete a password reset using the token from the email. |
| POST | `/send-verification-email` | `auth:api` | (Re)send the address-verification email. |
| POST | `/{installationKey}/verify-email` | installation key | Mark the email as verified using the token from the email. |

  </Tab>
  <Tab title="Impersonation" icon="user-secret">

Used by sales agents to act on behalf of a specific customer (cart, checkout, order history all become that customer's).

| Method | Endpoint | Auth | Description |
|---|---|---|---|
| GET  | `/impersonate/customers` | `auth:api` | List customers the agent is allowed to impersonate. |
| POST | `/impersonate/{customerId}` | `auth:api` | Start impersonating the given customer. |
| POST | `/impersonate/leave` | `auth:api` | Stop impersonating and return to the agent's own session. |

  </Tab>
  <Tab title="Account deletion" icon="user-xmark">

GDPR self-service deletion: the customer re-authenticates with their password, confirms with an emailed 6-digit code, and the account is soft-deleted after a grace period (default 14 days). Confirming also revokes every other active session.

| Method | Endpoint | Auth | Description |
|---|---|---|---|
| GET | `/me/deletion` | customer token | Current deletion request, or `{"deletion_request": null}`. |
| POST | `/me/deletion` | customer token | Start the flow — requires `current_password`; emails a 6-digit code (1h expiry). `409` when a deletion is already scheduled. Rate limited 5/min. |
| POST | `/me/deletion/confirm` | customer token | Confirm with `token` (the emailed code) — schedules the deletion after the grace period and revokes other sessions. Rate limited 10/min. |
| DELETE | `/me/deletion` | customer token | Cancel the pending or scheduled deletion. |
| GET | `/admin/customer/{customerId}/deletion-request` | admin (`auth-hub.account-deletion.view`) | Inspect a customer's deletion request. |
| DELETE | `/admin/customer/{customerId}/deletion-request` | admin (`auth-hub.account-deletion.manage`) | Cancel the request on the customer's behalf. |

<Callout kind="info">
The `deletion_request` payload contains `id`, `status` (`awaiting_confirmation` or `scheduled`), `requested_at`, `expires_at`, `confirmed_at`, `scheduled_for`, and `grace_days`. Every transition (code sent, scheduled, cancelled, executed) triggers a NotificationHub email to the customer.
</Callout>

  </Tab>
</Tabs>

## Related

- [Authentication](/authentication) — token model, headers, and storefront vs. admin flows.
- [Storefront / Customer Account](/advanced/storefront/customer-account) — end-to-end account lifecycle.
- [CustomerHub](/hubs/customer-hub) — the underlying customer records that AuthHub authenticates.
