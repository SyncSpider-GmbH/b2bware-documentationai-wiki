---
title: MCP Server
description: Connect AI assistants directly to your live DataHub data using the B2Bware MCP Server at mcp.b2bware.com.
---

## B2Bware MCP Server

The B2Bware MCP Server is a hosted remote service that connects AI assistants to your live DataHub using the [Model Context Protocol (MCP)](https://modelcontextprotocol.io). No local installation is required: configure your API key and interact with products, customers, orders, carts, checkout, and tax through natural language.

**Server URL:** `https://mcp.b2bware.com/sse`

<Callout kind="info" title="What is MCP?">
The Model Context Protocol (MCP) is an open standard that lets AI assistants call external tools and access data sources. With the B2Bware MCP Server connected, your assistant can browse the product catalog, search customers, manage carts, preview and complete checkout, calculate tax, and create or update orders — all in conversation.
</Callout>

## Available Tools

The MCP Server exposes **163 tools** and **4 workflow prompts** (v1.13.0) for server-to-server admin, integration, and operator analytics. Auth is **Bearer API key only** — no storefront `installation-key` or guest session headers. Order responses include a synthetic `order_number`; status `"processing"` is normalized from the API’s internal spelling.

<Tabs>
  <Tab title="Core" icon="box">

| Tool | Description |
|------|-------------|
| `search_products`, `list_products`, `get_product` | Product catalog read |
| `list_categories`, `get_category` | Category tree |
| `list_orders`, `get_order`, `update_order_status` | Order read/update (full status enum) |
| `list_carts`, `get_cart`, `add_cart_item`, `update_cart_item` | Cart operator queries + line-item API |
| `upsert_cart`, `remove_cart_item`, `preview_checkout`, `checkout` | Cart/checkout (admin/demo) |
| `list_shipping_methods`, `list_billing_methods` | Checkout prerequisites |
| `calculate_tax` | TaxHub preview |
| `list_customers`, `search_customers`, `get_customer`, `create_customer`, `update_customer` | CustomerHub |
| `list_customer_addresses`, `create_shipping_address`, `create_billing_address` | Addresses |
| `login`, `register` | AuthHub (legacy admin/demo) |

  </Tab>
  <Tab title="Catalog" icon="tag">

| Tool | Description |
|------|-------------|
| `create_category`, `update_category`, `delete_category`, `bulk_create_categories` | Category write |
| `create_product`, `update_product`, `delete_product`, `upsert_product`, `bulk_create_products` | Product write (SKU upsert) |
| `create_product_tier_price` | Quantity breaks |
| `attach_variation_attributes`, `generate_product_variations`, `list_product_variations` | Configurable products |
| `bulk_update_stock`, `bulk_upsert_prices` | ERP bulk sync |
| CDN media: `main_image_file`, `main_image_fetch_url` on create/update | ProductHub CDN ingest |

  </Tab>
  <Tab title="Attributes & B2B" icon="users">

| Tool | Description |
|------|-------------|
| `[attributes]` tools | AttributesHub — definitions (all storefront/admin flags), values, groups, `list_attribute_types`, sortable resync, entity assignments |
| `[customer]` company tools | `list_companies`, `create_company`, … (CustomerHub `/companies`) |
| `[b2b]` tools | Customer onboarding, roles, company linking |
| `[orders-admin]` tools | `create_order`, `create_order_comment`, demo catalog, backfill |

  </Tab>
  <Tab title="EventHub" icon="rss">

| Tool | Description |
|------|-------------|
| `list_available_events` | Event catalog for subscriptions |
| `list_event_subscriptions`, `get_event_subscription` | Subscription CRUD read |
| `create_event_subscription`, `update_event_subscription` | Webhook setup |
| `pause_event_subscription`, `resume_event_subscription`, `test_event_subscription` | Operations |
| `list_event_delivery_logs`, `get_event_delivery_log` | Delivery debugging |

  </Tab>
  <Tab title="RuleHub" icon="ticket">

| Tool | Description |
|------|-------------|
| `list_coupons`, `get_coupon`, `create_coupon`, `update_coupon` | Coupon management |
| `list_cart_price_rules`, `get_cart_price_rule` | Cart discounts |
| `list_cart_validation_rules` | Min-order etc. |
| `get_rule_condition_schema` | Condition builder fields |

  </Tab>
  <Tab title="Settings & Ops" icon="settings">

| Tool | Description |
|------|-------------|
| `list_locales`, `list_countries`, `list_currencies` | Reference data |
| `list_settings_config`, `get_settings_config` | Platform config |
| `clear_app_cache` | Ops — `installation_key` as URL path param only |
| `list_automation_flows`, `get_automation_flow`, `create_automation_flow` | AutomationHub |
| `list_available_automation_triggers`, `list_available_automation_actions` | Flow design |
| `list_flow_runs`, `get_flow_run` | Run monitoring |
| `list_dashboard_widgets`, `get_dashboard_layout` | Dashboard read-only |

  </Tab>
  <Tab title="Notify & License" icon="bell">

| Tool | Description |
|------|-------------|
| `list_notifications`, `get_notification`, `create_notification`, `send_notification` | NotificationHub |
| `list_notification_types`, `get_notification_stats` | Types and monitoring |
| `list_license_keys`, `get_license_key`, `generate_license_keys` | LicenseHub |
| `validate_license_key`, `activate_license_key` | Key lifecycle |
| `upload_media`, `list_media`, `get_media`, `set_primary_media` | MediaHub |

  </Tab>
</Tabs>

## Companion AI Skills

The MCP server exposes **tools** (live DataHub calls). **Skills** are separate Markdown guides that teach your assistant API rules, filters, validation, and integration patterns. For the best results, use both:

<Callout kind="tip" title="Recommended setup">
1. Connect this MCP server (Bearer API key) for live data and admin actions.
2. Install companion skills — see [Download AI Skill](/ai-tools/download-skill).
3. Copy the **b2bware-mcp** skill from the [b2bware-mcp](https://github.com/syncspider/b2bware-mcp) repository into `~/.cursor/skills/b2bware-mcp/` (tool usage, checkout flows, attribute patterns).
</Callout>

| Skill | Source | When to use |
|-------|--------|-------------|
| **b2bware-mcp** | [b2bware-mcp repo](https://github.com/syncspider/b2bware-mcp) → `.cursor/skills/b2bware-mcp/` | Calling MCP tools correctly (carts, checkout, catalog write, attributes) |
| **datahub-core** | [datahub-skills](https://github.com/SyncSpider-GmbH/datahub-skills) ZIP | Auth matrix, filtering, includes, pagination, errors |
| **datahub-hubs** | same repo | Full REST reference per hub (endpoints beyond MCP tools) |
| **datahub-storefront** | same repo | Headless storefront / portal (`installation-key`, guest carts) — **not** Bearer MCP |

Skills do not replace the MCP server and the MCP server does not ship the `datahub-*` bundles — install them separately. Overview: [AI Tools](/ai-tools/overview).

## Workflow Prompts (MCP)

Clients that support **MCP prompts** can fetch curated playbooks via `prompts/list` and `prompts/get` (no extra install). These guide the assistant through multi-step flows; tools still execute the API calls.

| Prompt | Purpose |
|--------|---------|
| `checkout_safe_flow` | Cart → `preview_checkout` → `checkout` with required IDs |
| `attribute_full_setup` | Definition, values, assignment (AttributesHub + ProductHub quirks) |
| `b2b_customer_onboarding` | Company, addresses, customer, company link |
| `erp_catalog_sync` | Categories, SKU upsert, bulk stock/prices |

Optional arguments personalize context (e.g. `checkout_safe_flow` with `customer_id`, `cart_id`). Cursor **skills** remain recommended for day-to-day use; prompts help Claude Desktop and other MCP clients without skill files.

## Getting Your API Key

<Steps>
  <Step title="Log in to B2Bware" icon="log-in">
    Go to your B2Bware admin panel.
  </Step>
  <Step title="Navigate to API settings" icon="settings">
    Go to **Settings → API → API Keys**.
  </Step>
  <Step title="Create or copy your API key" icon="key">
    Create a new API key or copy an existing one. This key works for both the REST API and the MCP Server.
  </Step>
</Steps>

## Authentication Methods

<Tabs>
  <Tab title="Header-Based (Recommended)" icon="shield">

Pass your API key via the `Authorization` header:

```
URL: https://mcp.b2bware.com/sse
Header: Authorization: Bearer YOUR_B2BWARE_API_KEY
```

<Callout kind="tip">
The header approach is more secure since the API key does not appear in server logs or browser history.
</Callout>

  </Tab>
  <Tab title="URL Parameter" icon="link">

For clients that do not support custom headers, pass the API key as a query parameter:

```
https://mcp.b2bware.com/sse?key=YOUR_B2BWARE_API_KEY
```

Use URL parameters only when custom headers are not supported by your client.

  </Tab>
</Tabs>

## Client Configuration

<Tabs>
  <Tab title="Cursor" icon="code">

**File:** `~/.cursor/mcp.json` (Mac) or `%USERPROFILE%\.cursor\mcp.json` (Windows)

```json
{
  "mcpServers": {
    "b2bware": {
      "url": "https://mcp.b2bware.com/sse",
      "headers": {
        "Authorization": "Bearer YOUR_B2BWARE_API_KEY"
      }
    }
  }
}
```

<Steps>
  <Step title="Open or create the config file" icon="file">
    Create `mcp.json` in your Cursor config directory if it doesn't exist.
  </Step>
  <Step title="Paste the configuration" icon="clipboard">
    Replace `YOUR_B2BWARE_API_KEY` with your actual API key.
  </Step>
  <Step title="Restart Cursor" icon="refresh-cw">
    Restart Cursor to load the MCP server. You should see the B2Bware tools in the MCP panel.
  </Step>
</Steps>

  </Tab>
  <Tab title="Claude Desktop" icon="monitor">

**File:** `~/Library/Application Support/Claude/claude_desktop_config.json` (Mac) or `%APPDATA%\Claude\claude_desktop_config.json` (Windows)

```json
{
  "mcpServers": {
    "b2bware": {
      "url": "https://mcp.b2bware.com/sse",
      "headers": {
        "Authorization": "Bearer YOUR_B2BWARE_API_KEY"
      }
    }
  }
}
```

<Steps>
  <Step title="Open Claude Desktop settings" icon="settings">
    Open Claude Desktop and go to settings, or edit the config file directly.
  </Step>
  <Step title="Add the MCP server" icon="plus">
    Paste the configuration above with your API key.
  </Step>
  <Step title="Restart Claude Desktop" icon="refresh-cw">
    Restart the app. The B2Bware tools will appear in the tool list.
  </Step>
</Steps>

  </Tab>
  <Tab title="Claude Code" icon="terminal">

Add to your project's `.mcp.json` or Claude Code settings:

```json
{
  "inputs": [
    {
      "id": "api_key",
      "type": "promptString",
      "description": "B2Bware API Key",
      "password": true
    }
  ],
  "servers": {
    "b2bware": {
      "type": "sse",
      "url": "https://mcp.b2bware.com/sse",
      "headers": {
        "Authorization": "Bearer ${input:api_key}"
      }
    }
  }
}
```

<Callout kind="tip">
Claude Code prompts you for the API key securely at startup. The key is never stored in plain text.
</Callout>

  </Tab>
  <Tab title="Windsurf" icon="wind">

**File:** `~/.codeium/windsurf/mcp_config.json` (Mac) or `%USERPROFILE%\.codeium\windsurf\mcp_config.json` (Windows)

```json
{
  "mcpServers": {
    "b2bware": {
      "serverUrl": "https://mcp.b2bware.com/sse",
      "headers": {
        "Authorization": "Bearer YOUR_B2BWARE_API_KEY"
      }
    }
  }
}
```

<Callout kind="alert" title="Note">
Windsurf uses `serverUrl` instead of `url`. Make sure to use the correct key name.
</Callout>

  </Tab>
  <Tab title="Claude.ai" icon="globe">

For Claude.ai (web), use the URL parameter method:

```
https://mcp.b2bware.com/mcp?key=YOUR_B2BWARE_API_KEY
```

**With language preference:**
```
https://mcp.b2bware.com/sse?key=YOUR_B2BWARE_API_KEY&lang=de
```

Supported languages: `de` (German), `en` (English), `fr` (French), `es` (Spanish)

<Callout kind="info">
Leave the OAuth fields empty when adding this as an MCP integration in Claude.ai. The API key in the URL is sufficient for authentication.
</Callout>

  </Tab>
</Tabs>

## Security

<Callout kind="alert" title="Connection Isolation">
Every connection is fully isolated by your API key. The server stores no keys — they only live in memory for the duration of the active session and are discarded immediately on disconnect.
</Callout>

- API keys are never persisted on the MCP server
- Each session is isolated — no cross-session data leakage
- Prefer header-based authentication over URL parameters
- Never commit API keys to version control — use environment variables or secure input prompts

## Example Prompts

Once connected, try these prompts with your AI assistant:

| Prompt | What it does |
|--------|--------------|
| "Show me all products in the Electronics category" | `list_products` with category filter (e.g. anchor_category_id) |
| "Search for customers from Berlin" | `search_customers` |
| "What's the status of order #1234?" | `get_order` with ID |
| "Create or update a cart with 3x Product SKU-001 and 1x SKU-002" | `upsert_cart` with cart_items |
| "Show me the latest order" | `list_orders` with `sort=-id`, `per_page=1` |
| "Get customer 42 with all addresses" | `get_customer` (returns shipping and billing addresses) |
| "Show me the checkout total for cart 17 before I place the order" | `preview_checkout` with cart_id |
| "Calculate tax for two line items (100 € and 50 €) for a customer in Germany" | `calculate_tax` with line_items and customer.country |
| "Show me all pending orders from this month" | `list_orders` with status and date filters |
| "Show me open carts" | `list_carts` |
| "Create a webhook for order.created" | `create_event_subscription` |
| "Generate 50 license keys for product 12" | `generate_license_keys` |

## Troubleshooting

<Expandable title="Server not connecting" default-open="false">

1. Verify your API key is correct (test it with a direct API call first)
2. Check the server status at [mcp.b2bware.com](https://mcp.b2bware.com)
3. Ensure your client supports SSE (Server-Sent Events) transport
4. Restart your AI tool after changing the configuration

</Expandable>

<Expandable title="Tools not appearing" default-open="false">

1. Check that the config file is in the correct location for your tool
2. Verify the JSON syntax is valid (no trailing commas, correct quotes)
3. For Windsurf: use `serverUrl` instead of `url`
4. Restart the application after saving the config

</Expandable>
